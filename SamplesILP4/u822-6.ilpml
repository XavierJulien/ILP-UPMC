class Point extends Object
{
    var x;
    var y;

    method longueur ()
    self.x + self.y;

    method m1 (z,t)
    z * self.x + t * self.y;
}

class PointColore extends Point
{
    var color;
}

let pc = new PointColore(11, 22, "red") in
(
  pc.color = 822;
  pc.color
)
