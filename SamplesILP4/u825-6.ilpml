class Point extends Object
{
    var x;
    var y;

    method longueur ()
    self.x + self.y;

    method m1 (z,t)
    z * self.x + t * self.y;
}

class PointColore extends Point
{
    var color;

    method m2 () 824;
}

let pc = new PointColore(820, 5, "red") in
pc.m1(1, 1)
